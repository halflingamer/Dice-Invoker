"use client";
import{useState}from"react";import{DiceTray,type DisplayDie}from"./DiceTray";import{EssenceMeter}from"./EssenceMeter";import{HeroSheet}from"./HeroSheet";import{PhaserBattle,type BattleAnimationEvent}from"./PhaserBattle";import{RunMap}from"./RunMap";
const initialDice:DisplayDie[]=[{id:"rusty-sword",name:"Espada Enferrujada",face:"Corte",value:5,locked:false},{id:"wooden-shield",name:"Escudo de Madeira",face:"Aparar",value:2,locked:false}];
export function CombatStage(){const[dice,setDice]=useState(initialDice);const[essence,setEssence]=useState(2);const[enemyHp,setEnemyHp]=useState(18);const[event,setEvent]=useState<BattleAnimationEvent|null>(null);const[message,setMessage]=useState("Escolha o destino dos dados.");const animate=(type:BattleAnimationEvent["type"])=>setEvent({id:Date.now(),type});const lock=(id:string)=>{setDice(ds=>ds.map(d=>d.id===id?{...d,locked:true}:d));setMessage("Resultado travado.")};const reroll=()=>{if(essence<1)return;setEssence(v=>v-1);setDice(ds=>ds.map(d=>d.locked?d:{...d,value:d.value===6?1:d.value+1,face:d.id==="rusty-sword"?"Golpe":"Firmar"}));animate("reroll");setMessage("O destino foi rerrolado.")};const activate=()=>{const damage=dice.find(d=>d.id==="rusty-sword")?.value??0;setEnemyHp(v=>Math.max(0,v-damage));animate("hit");setDice(ds=>ds.map(d=>({...d,locked:false})));setMessage(enemyHp-damage<=0?"Slime derrotado! Recompensa liberada.":`Escudeiro causou ${damage} de dano.`)};return <main className="game-shell"><HeroSheet/><section className="battle-column"><header className="combat-header"><div><b>Escudeiro</b><span>♥ 28/40</span></div><div className="room-progress"><b>Sala 3/10</b><span>● ● ● ○ ○ ○ ○ ○ ○ ○</span></div><div className="enemy"><b>Slime de Recibo</b><span>♥ {enemyHp}/18</span></div></header><PhaserBattle event={event}/><div className="status-line" aria-live="polite">{message}<EssenceMeter value={essence} max={2}/></div><DiceTray dice={dice} essence={essence} busy={false} onLock={lock} onReroll={reroll} onActivate={activate}/></section><RunMap/></main>}
