"use client";
import{useCallback,useState}from"react";export function useRunCommand(runId:string){const[busy,setBusy]=useState(false);const send=useCallback(async(command:Record<string,unknown>)=>{setBusy(true);try{const response=await fetch(`/api/runs/${runId}/commands`,{method:"POST",headers:{"Content-Type":"application/json","Idempotency-Key":crypto.randomUUID()},body:JSON.stringify(command)});if(response.status===409){await fetch(`/api/runs/${runId}`,{cache:"no-store"});throw new Error("Estado atualizado pelo servidor; tente novamente.")}if(!response.ok)throw new Error("Comando recusado pelo servidor.");return response.json()}finally{setBusy(false)}},[runId]);return{busy,send}}
